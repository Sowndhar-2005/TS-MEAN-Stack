

# Server Configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/campus-canteen

# JWT Secret (Change this in production!)
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=24h

# Client URL (for CORS)
CLIENT_URL=http://localhost:4200

# College Validation
COLLEGE_EMAIL_DOMAIN=@college.edu
REGISTRATION_NUMBER_PATTERN=^[0-9]{2}[A-Z]{3}[0-9]{3}$

# Default Wallet Balance (in rupees)
DEFAULT_WALLET_BALANCE=3000

# Payment Gateway (Mock for development)
UPI_GATEWAY_URL=https://mock-upi-gateway.com
